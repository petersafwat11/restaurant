// TODO(seo): item detail SEO route. Real customization opens the
// ItemDetailSheet over /menu instead. This route may render a server-side
// fallback for direct link sharing (Phase 4+).
export default function MenuItemPage() {
  return null;
}
