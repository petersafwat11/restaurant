# Components

UI components live here in the UI sprint. **Do not add components yet.**

Shared components come from `@repo/ui` (built on shadcn/ui). Per-screen
components go in `src/features/<feature>/components/`.
